<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wetour.we_t">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WE_T">
        <activity android:name=".ui.placeDetail.PlaceDetailActivity"></activity>
        <activity android:name=".ui.placeHome.PlaceHomeActivity" />
        <activity android:name=".ui.placeInfo.PlaceInfoActivity" />
        <activity android:name=".ui.makeTour.InviteFamilyActivity" />
        <activity android:name=".ui.makeTour.selectTourLocation.SelectTourLocationActivity" />
        <activity android:name=".ui.makeTour.MakeTourActivity" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.user.SelectSignInMode" />
        <activity android:name=".ui.user.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>